# GOrchestrator Configuration
# Copy this file to .env and update values as needed

# ============================================================
# ORCHESTRATOR (Manager Agent) Configuration
# The Manager is the intelligent LLM that talks to users and
# decides when to delegate tasks to the Worker.
# ============================================================

# LLM model for the Manager Agent
ORCHESTRATOR_MODEL=claude-3-5-sonnet-20241022

# API endpoint for the Manager's LLM calls
ORCHESTRATOR_API_BASE=http://127.0.0.1:8045

# API key for the Manager's LLM
ORCHESTRATOR_API_KEY=sk-dummy-orchestrator-key

# ============================================================
# WORKER (Mini-SWE-GOCore) Configuration
# The Worker is the coding agent that executes tasks.
# ============================================================

# LLM model passed to the Worker subprocess
WORKER_MODEL=claude-3-5-sonnet-20241022

# Configuration profile for the Worker agent
# Available profiles depend on Mini-SWE-GOCore configuration:
#   - 'live': Standard production mode (default)
#   - 'swebench': SWE-Bench evaluation mode
#   - 'custom': Custom profile defined in Mini-SWE-GOCore
WORKER_PROFILE=live

# Path to Mini-SWE-GOCore folder (relative or absolute)
AGENT_PATH=../Mini-SWE-GOCore

# Proxy configuration for Worker subprocess
PROXY_URL=http://127.0.0.1:8045
PROXY_KEY=sk-dummy-proxy-key

# Bypass key for LiteLLM (critical for direct API access)
BYPASS_KEY=sk-dummy

# ============================================================
# Application Settings
# ============================================================

# Show detailed Worker output (true/false)
VERBOSE_WORKER=false

# Maximum Worker retry iterations per task
MAX_WORKER_ITERATIONS=5

# Maximum time in seconds for a Worker task before timeout (0 = no timeout)
WORKER_TIMEOUT=600
